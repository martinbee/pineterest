<div class="row">
  <% @pins.each do |pin| %>
    <div class="col-s-12 col-md-4 col-md-offset-1">
      <div class="thumbnail <%= pin.id %>-pin">
        <%= link_to (image_tag pin.photo.url(:pin)), [:open_pin_modal, id: pin.id], remote: true %>
      </div>
    </div>
  <% end %>
</div>
